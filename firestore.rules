rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access only for authenticated users.
    // This is a basic security rule for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
