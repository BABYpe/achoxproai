export const APP_NAME = 'AchoX Pro';
export const APP_VERSION = '1.1.0';

export const API_BASE_URL = '/api';

export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
  PROJECTS: '/dashboard/projects',
  SETTINGS: '/dashboard/settings',
};

// Default values and limits
export const DEFAULT_PAGE_SIZE = 10;
export const MAX_FILE_UPLOAD_SIZE_MB = 25;

// Cookie names
export const AUTH_TOKEN_COOKIE = 'achox_auth_token';
export const THEME_COOKIE = 'achox_theme';

// Local storage keys
export const USER_PREFERENCES_KEY = 'achox_user_prefs';
